#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "Theatre_Explorer"

# You can add fixtures and/or initialization code here to make experimenting
# with your gem easier. You can also use a different console, if you like.


# (If you use this, don't forget to add pry to your Gemfile!)
require "pry"

require "irb"

def reload!(print = true)
    puts 'Reloading ...' if print
    # Main project directory.
    root_dir = File.expand_path('..', __dir__)
    # Directories within the project that should be reloaded.
    reload_dirs = %w{lib}
    # Loop through and reload every file in all relevant project directories.
    reload_dirs.each do |dir|
        Dir.glob("#{root_dir}/#{dir}/**/*.rb").each { |f| load(f) }
    end
    # Return true when complete.
    true
end

Year.new('1999')
Year.new('2004')
Year.new('2008')
Year.new('2019')
Year.new('1987')

Scraper.new.production("https://www.broadwayworld.com/shows/Chicago-3959.html")
Scraper.new.production("https://www.broadwayworld.com/shows/backstage.php?showid=311650")
Scraper.new.production("https://www.broadwayworld.com/shows/backstage.php?showid=318163")
Scraper.new.production("https://www.broadwayworld.com/shows/?showid=313883")
Scraper.new.production("https://www.broadwayworld.com/shows/?showid=312546")
Scraper.new.production("https://www.broadwayworld.com/shows/?showid=312524")


IRB.start(__FILE__)
